# Optional. Raises the GitHub API rate limit from 10 to 30 search req/min
# and 60 to 5000 core req/hour. A classic token with NO scopes is enough —
# this project only reads public data. Never commit the real value.
GITHUB_TOKEN=
